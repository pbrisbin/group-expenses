<div class="group">
  <h4 class="subheader"><%= group.name %></h4>

  <div class="situation">
    <p>You owe so-and-so $X</p>
    <p>So-and-so owes you $Y</p>
  </div>

  <div class="actions">
    <%= link_to "New expense", new_expense_path(:group_id => group.id) %> | 
    <%= link_to "Join link", new_membership_url(:token => group.join_token) %> | 
    <%= link_to "Leave group", "#" %>
  </div>

</div>
